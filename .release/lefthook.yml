# Generated by release-sync from arthur-debert/release@release-core. Do not edit.
# Regenerate by running release-sync.

output:
  - meta
  - skips
  - summary
pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "**/*.md"
      run: >-
        if command -v markdownlint >/dev/null 2>&1; then

          markdownlint --fix -- {staged_files};
        else

          echo "ERROR: markdownlint not installed — the gate does not skip. Re-run the bootstrap (setup-dev-env.sh)." >&2;
          exit 1;
        fi
      stage_fixed: true
      priority: 2
    yamllint:
      glob: "**/*.{yml,yaml}"
      run: >-
        if command -v yamllint >/dev/null 2>&1; then

          yamllint -- {staged_files};
        else

          echo "ERROR: yamllint not installed — the gate does not skip. Re-run the bootstrap (setup-dev-env.sh)." >&2;
          exit 1;
        fi
      priority: 2
    shellcheck:
      glob:
        - "**/*.{sh,bash}"
        - "bin/**"
        - "app-bin/**"
      run: bin/check-shell {staged_files}
      priority: 2
    eslint:
      glob:
        - "**/*.{js,jsx,ts,tsx,vue,mjs,cjs}"
        - "**/.eslintrc*"
        - "**/eslint.config.*"
      run: npx --no-install eslint --no-warn-ignored --max-warnings 0 -- {staged_files}
      priority: 2
    prettier:
      glob:
        - "**/*.{js,jsx,ts,tsx,vue,svelte,json,html,css,scss,md,yml,yaml}"
      run: >-
        set --; for f in {staged_files}; do

          [ -L "$f" ] || set -- "$@" "$f";
        done; if [ $# -gt 0 ]; then

          npx --no-install prettier --write -- "$@";
        fi
      stage_fixed: true
      priority: 2
    typecheck:
      glob:
        - "**/*.{ts,tsx}"
        - "**/tsconfig*.json"
      run: npm run --silent typecheck --if-present
      priority: 2
